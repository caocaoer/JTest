import React from 'react';

const Index = (props) => {
    return (
        <div>
            <button>
                {props.text}
            </button>
        </div>
    );
}

export default Index;